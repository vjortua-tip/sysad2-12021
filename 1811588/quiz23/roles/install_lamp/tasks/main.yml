---
# tasks file for roles/install_lamp

- name: Apache2 Installation
  become: yes
  become_user: root
  service:
    name: apache2
    state: started
    enabled: yes

- name: MySQL Installation
  become: yes
  become_user: root
  service:
    name: mysql
    state: started
    enabled: yes

- name: Directory
  file: path=/var/www/html state=directory mode=0755

- name: HTML Installation
  become: yes
  become_user: root
  copy:
    src: /tmp/index.html
    dest: /var/www/html/index/html
